{
  "name": "fe",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && swc src -d dist && cp -R public/* dist/public",
    "build:watch": "rm -rf dist && swc src -d dist && cp -R public/* dist/public -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@swc/cli": "^0.7.9",
    "@swc/core": "^1.15.2"
  }
}
