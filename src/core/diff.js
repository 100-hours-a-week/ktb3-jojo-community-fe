/**
 *
 * @param {} oldVNode
 * @param {} newVNode
 */

export function diff(oldVNode, newVNode) {
  //TODO: 추상화를 코드로 구현
  //tag 차이 -> mount 위한 새로운 vNode 생성
  //state, prop 차이 -> 기존 것에 변경
  //children 도 diff 재귀적으로
  //모든 변경 사항 있는 update Queue 반환
}
